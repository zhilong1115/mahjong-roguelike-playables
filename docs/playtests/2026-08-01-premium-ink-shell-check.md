# 2026-08-01 · 成品化水墨外层检查

类型：实现检查 + 自动屏幕矩阵 + 开发者视觉巡检
对应决定：`0032-premium-ink-game-shell.md`（`Proposed`）

## 本轮问题

已有水墨素材与牌面具备方向，但标题、选关和牌桌仍呈现为多个网页面板，缺少完整小游戏的品牌入口、路线感和实体牌具层级。

## 实现范围

- 标题页改为横屏左右、竖屏上下的“游戏扉页”：品牌场景与宣纸菜单分开。
- 现有闲局、庄局、圈主三关改成墨线连接的行旅路线，并显示当前 / 待战 / 已过 / 已跳。
- 左侧四组状态收进同一根旧木状态轨，牌桌、锦囊、操作区和手牌托条按实体材质重新分层。
- 没有修改 `src/core/`、`src/content/`、`src/state/`、规则数值或存档结构。

## 自动检查

- `node --test tests/src-*.test.mjs`：96 / 96 通过。
- `node --test tests/*.test.mjs`：149 / 149 通过。
- `node tests/src-viewport-smoke.mjs`：通过 required 六视口与 667×375 扩展横屏；无页面滚动、关键裁切或被遮挡操作。
- 新增断言确认标题品牌 / 菜单分层、三关节点、当前关状态及路线说明存在。
- 844×390 牌桌仍保持 14 张手牌单排；360×800 的标题菜单与关卡路线不需要横向页面滚动。

## 开发者视觉巡检

- 844×390：标题从居中矩形面板变成左侧品牌、右侧菜单的完整场景；三关路线的前后关系和圈主压轴更明确。
- 360×800：标题改为上下构图，主菜单与路线节点仍在自然纵向阅读流中。
- 844×390 牌桌：四组状态不再像四个独立网页卡片，手牌与可操作区成为第一视觉焦点。

## 尚未验证

“精美”是审美判断，自动测试只能证明结构与可用性。本决定继续保持 `Proposed`，等待手机真机确认：

1. 标题是否已经像一款可发布小游戏，而不是网页 Demo。
2. 路线页是否能在 3 秒内指出当前关、下一关与圈主。
3. 局内木色 / 宣纸 / 朱砂的对比是否舒适，是否仍有区域显得过重或过灰。
