# 2026-07-31 · 窄横屏最小修复复核

关联决定：`0028-minimal-compact-landscape-hand-fix.md`

## 问题与成功信号

用户真机否决 `0027`：虽然第 14 张不再换行，但顶部横向构筑带让牌桌变矮、信息变碎，整体不如旧版。
本轮要求恢复左侧状态 / 构筑、右侧牌桌的熟悉层级，只在窄横屏解决 14 张换行；同时不能恢复旧版 844×390
左栏底部被静默裁掉的缺陷。

## 实现

- 撤掉 `#handDock` 和顶部构筑带；操作栏、手牌回到 `#main`，构筑与四项状态回到左栏。
- 矮横屏把关卡、牌势、资源、构筑四组内部压紧，使常驻构筑底边落在视口内。
- 667×375 再把左栏缩到 116px，开运位压到 68px 档，相邻手牌叠边 7px；点击盒仍为 44px。
- smoke 将 `#buildPanel` 列为关键区域，直接检查旧层级、14 张单排、无遮挡与中心点命中。

## 自动结果

- 正式版规则 / 集成：84 / 84 通过。
- 全仓规则 / 集成：137 / 137 通过。
- required 六视口与 667×375 扩展视口全部通过：无页面滚动、关键裁切、中心遮挡或状态重置。
- 844×390：主区 682px、左栏 150px、14 张一排、最小手牌点击宽 45px、开运位 72px 高。
- 667×375：主区 539px、左栏 116px、14 张一排、最小点击盒 44×44px、开运位 58px 高。

## 视觉复核

- 844×390：左栏四组重新形成清楚纵向层级，顶部只剩六个开运位；常驻构筑底边为 386px，完整落在 390px 视口内。
- 667×375：14 张牌在底部单排，牌面主体仍可读；中央牌桌和开运位没有被新的横向构筑条挤压。
- 360×800：仍为 7×2 手牌，左栏转为顶部信息层；单牌亮组和签面没有因横屏修复改变。

## 仍待真人判断

自动检查只能证明可见和可操作。窄横屏叠边后的识牌、连续点选和拖拽手感仍需手机复测；
如果不舒服，按 `0028` 回退条件尝试仅让手牌局部延伸到左栏下方，不再重做整套 HUD。
